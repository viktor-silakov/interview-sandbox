/**
 * Write the function that takes array of numbers
 * and return array only odd numbers from the argument array
 */

function returnOdd(arr) {
  return filtered_array;
}

returnOdd([1, 2, 3]); // expected [1,3]
returnOdd([1, 2, 3, 4, 5]); // expected [1,3,5]
